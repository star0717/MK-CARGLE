{
  "dependencies": {
    "@nestjs/mapped-types": "*"
  },
  "name": "n2server",
  "description": "Next.js + Nestjs 기반의 통합서버",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "local:build": "docker-compose build",
    "local:up": "docker-compose up",
    "local:up:build": "docker-compose up --build",
    "local:up:daemon": "docker-compose up -d",
    "local:down": "docker-compose down",
    "local:logs": "docker-compose logs",
    "local:rm": "docker-compose rm",
    "local:port": "docker-compose port",
    "local:image:prune": "sudo docker image prune",
    "local:image:rmAll": "sudo docker rmi $(docker images -q)",
    "local:container:prune": "sudo docker container prune",
    "ecs:login": "aws ecr get-login-password --region ap-northeast-2 | docker login --username AWS --password-stdin 151333897315.dkr.ecr.ap-northeast-2.amazonaws.com",
    "ecs:build": "docker-compose --file docker-compose.ecs.yml build",
    "ecs:push": "docker-compose --file docker-compose.ecs.yml push",
    "ecs:up": "docker-compose --file docker-compose.ecs.yml up"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ByunMooYoung/n2server.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ByunMooYoung/n2server/issues"
  },
  "homepage": "https://github.com/ByunMooYoung/n2server#readme"
}

