/**
 * url query 정의
 */
export enum SignRoute {
  SIGNUP = "signup",
  FIND = "find",
}

export enum MainRoute {
  MAIN = "main",
  MYPAGE = "mypage",
}

export enum SubRoute {
  ACCOUNT = "account",
  WORKER = "worker",
}
